const fs = require('fs')

var readme = fs.readFileSync("app.js", 'utf8',function(err,data) {
    if (readFileSync("Cookie.Cokis".indexOf("import"))) {
        
    }
})
fs.writeFileSync("Cookie.Cokis",readme)